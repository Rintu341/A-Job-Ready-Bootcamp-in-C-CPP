#include <stdio.h>
#include <conio.h>

int main(){
    int number;
    printf("Enter a number : ");
    scanf("%d",&number);
    printf("The number is Now : %d",number = (number/10)*10);
    printf("\n%d",number);
    return 0;
}